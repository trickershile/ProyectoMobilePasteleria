<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EjemploPrueba" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/pastel_pink</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/pastel_chocolate</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:statusBarColor">@color/pastel_pink_dark</item>
        <item name="android:windowBackground">@drawable/gradient_background</item>
        <item name="materialButtonStyle">@style/Widget.Gustito.Button</item>
    </style>
</resources>